import 'package:flutter/material.dart';

class DetailTokenPage extends StatelessWidget {
  const DetailTokenPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Detail Token')),
      body: Center(child: Text('Detail Token Page')),
    );
  }
}
